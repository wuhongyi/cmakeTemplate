CMAKE脑残化运行教程：
####################################################################
在本文件目录下打开终端，运行脚本文件:
. cmahe.sh

程序运行结束显示可执行文件名称，直接运行可执行文件:
./可执行文件名

####################################################################
文件夹bin是用来放置生成的可执行文件，
将所需要引用的.hh，.h文件放在文件夹include，
将所需要引用的.cc，.c文件放在文件夹src，
将所需要引用的.so，.a文件放在文件夹lib。
主程序放在本文件所在目录（例如：input.cc），数据文件也放在本文件所在目录（例如：data1.dat data2.dat)。
修改CMakeLists.txt里面相应的设置即可。
